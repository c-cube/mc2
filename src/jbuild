; vim:ft=lisp:

(jbuild_version 1)

; main binary
(executable
  ((name main)
   (public_name minismt)
   (libraries (dolmen containers sequence
               minismt.core minismt.sat minismt.backend))
   (flags (:standard -w +a-4-42-44-48-50-58-32-60@8 -color always))
   (ocamlopt_flags (:standard -O3
                    -unbox-closures -unbox-closures-factor 20))
  ))
