; vim:ft=lisp:

(jbuild_version 1)

; main binary
(executable
  ((name main)
   (public_name mc2)
   (libraries (dolmen containers sequence result
               mc2.core mc2.backend mc2.smtlib))
   (flags (:standard -w +a-4-42-44-48-50-58-32-60@8 -color always))
   (ocamlopt_flags (:standard -O3
                    -unbox-closures -unbox-closures-factor 20))
  ))

(alias
 ((name    runtest)
  (deps    (tests/test_api.exe))
  (action  (run ${<}))))

